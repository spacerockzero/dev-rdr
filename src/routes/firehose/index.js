import { h } from 'preact';
import ArticleList from '../../components/ArticleList';
import style from './style';

class Firehose extends ArticleList {
	constructor(props) {
		super(props);
	}
}

export default Firehose;
